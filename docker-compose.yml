services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/dm_oracle.db
    volumes:
      - dm_oracle_data:/data
    command: ["python", "main.py"]

volumes:
  dm_oracle_data:
